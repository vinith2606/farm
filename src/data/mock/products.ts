/** @deprecated Use `@/data` instead — kept for backward compatibility */
export {
  products as mockProducts,
  categories,
  farmerMarkers,
  marketTrends,
  revenueData,
  userGrowthData,
} from '@/data'
